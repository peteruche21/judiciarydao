{
  "name": "judiciarydao",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "next-app",
    "smart-contracts"
  ],
  "scripts": {
    "dev": "yarn workspace next-app next dev",
    "build": "yarn workspace next-app next build",
    "start": "yarn workspace next-app next start",
    "lint": "yarn workspace next-app next lint",
    "hardhat": "yarn workspace smart-contracts hardhat"
  },
  "devDependencies": {
    "ts-node": "^10.7.0",
    "typescript": "^4.6.4"
  }
}
